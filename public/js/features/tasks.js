import { getState, setState } from '../core/store.js';

const uid = (prefix) => `${prefix}-${Date.now()}-${Math.random().toString(36).slice(2, 7)}`;
const today = () => new Date().toISOString().slice(0, 10);
const esc = (value = '') => String(value).replace(/[&<>\"']/g, (char) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[char]));

function dateLabel(value) { if (!value) return 'Không có deadline'; const date = new Date(`${value}T00:00:00`); if (Number.isNaN(date.getTime())) return value; return date.toLocaleDateString('vi-VN', { day: '2-digit', month: '2-digit', year: 'numeric' }); }
function dayDiff(from, to) { if (!from || !to) return null; const a = new Date(`${from}T00:00:00`), b = new Date(`${to}T00:00:00`); if (Number.isNaN(a.getTime()) || Number.isNaN(b.getTime())) return null; return Math.round((b - a) / 86400000); }
function normalizeTask(task) { const dueDate = task.dueDate || task.due || ''; return { ...task, startDate: task.startDate || dueDate || '', dueDate, dueTime: task.dueTime || '', priority: task.priority || 'medium', done: Boolean(task.done) }; }
function deadlineStatus(task) { if (task.done) return '✓ Hoàn thành'; if (!task.dueDate) return ''; const due = new Date(`${task.dueDate}T${task.dueTime || '23:59'}:00`), diff = due.getTime() - Date.now(); if (Number.isNaN(due.getTime())) return ''; if (diff < 0) { const hours = Math.floor(Math.abs(diff) / 3600000); if (hours < 24 && task.dueTime) return `⚠ Quá hạn ${hours} giờ`; return `⚠ Quá hạn ${Math.floor(Math.abs(diff) / 86400000)} ngày`; } if (diff <= 86400000 && task.dueTime) { const totalMinutes = Math.floor(diff / 60000), hours = Math.floor(totalMinutes / 60), minutes = totalMinutes % 60; return `⏱ Còn ${hours} giờ ${minutes} phút`; } const days = Math.ceil(diff / 86400000); return days === 0 ? '⚠ Deadline hôm nay' : `Còn ${days} ngày`; }
function within24h(task) { if (task.done || !task.dueDate || !task.dueTime) return false; const due = new Date(`${task.dueDate}T${task.dueTime}:00`).getTime(); const diff = due - Date.now(); return diff >= 0 && diff <= 86400000; }
function startCountdownTicker() { clearInterval(window.__scholarTaskTicker); window.__scholarTaskTicker = setInterval(() => { if (document.querySelector('#appView .task-list')) renderCurrentTaskList(); }, 30000); }
function renderCurrentTaskList() { const root = document.querySelector('#appView .task-list'); if (!root) return; const result = tasks(); root.outerHTML = result.render().match(/<div class="task-list">[\s\S]*<\/div>$/)?.[0] || root.outerHTML; }

export function tasks() {
  const items = (getState().tasks || []).map(normalizeTask), now = today(), filter = window.__scholarTaskFilter || 'all';
  const filtered = items.filter((task) => { const due = task.dueDate; if (filter === 'today') return due === now || (task.startDate && task.startDate <= now && due >= now); if (filter === 'upcoming') return !task.done && within24h(task); if (filter === 'overdue') return !task.done && due && due < now; if (filter === 'completed') return task.done; return true; });
  filtered.sort((a, b) => String(a.dueDate || '9999').localeCompare(String(b.dueDate || '9999')));
  startCountdownTicker();
  return { title: 'Nhiệm vụ', description: 'Theo dõi việc cần làm bằng khoảng thời gian và deadline rõ ràng.', render: () => `<div class="module-toolbar task-toolbar"><div><h2 class="section-title">Danh sách nhiệm vụ</h2><p class="muted">Một nhiệm vụ có thể kéo dài nhiều ngày cho tới deadline.</p></div><button class="button primary" data-action="task-add">+ Thêm nhiệm vụ</button></div><div class="task-filters" role="toolbar" aria-label="Lọc nhiệm vụ">${[['all','Tất cả'],['today','Hôm nay'],['upcoming','Trong vòng 24h'],['overdue','Quá hạn'],['completed','Hoàn thành']].map(([key,label]) => `<button type="button" class="task-filter ${filter === key ? 'active' : ''}" data-action="task-filter" data-filter="${key}">${label}</button>`).join('')}</div><div class="task-list">${filtered.length ? filtered.map((task) => { const span = dayDiff(task.startDate, task.dueDate); let deadlineText = `Deadline: ${dateLabel(task.dueDate)}`; if (span !== null && span > 0) deadlineText = `${dateLabel(task.startDate)} → ${dateLabel(task.dueDate)}`; if (task.dueTime) deadlineText += ` · ${task.dueTime}`; const status = deadlineStatus(task); const overdue = !task.done && task.dueDate && new Date(`${task.dueDate}T${task.dueTime || '23:59'}:00`) < new Date(); return `<article class="task-card ${task.done ? 'is-done' : ''} ${overdue ? 'is-overdue' : ''}"><button type="button" class="check-button" data-action="task-toggle" data-id="${task.id}" aria-label="Hoàn thành">${task.done ? '✓' : ''}</button><div class="task-main"><div class="task-title-row"><strong>${esc(task.title)}</strong><span class="priority priority-${esc(task.priority)}">${esc(task.priority)}</span></div>${task.description ? `<p>${esc(task.description)}</p>` : ''}<div class="task-meta"><span>📅 ${deadlineText}</span>${status ? `<span class="task-status">${status}</span>` : ''}</div></div><button type="button" class="icon-button" data-action="task-delete" data-id="${task.id}" aria-label="Xóa">×</button></article>`; }).join('') : `<div class="empty-state"><strong>${filter === 'overdue' ? 'Không có nhiệm vụ quá hạn' : filter === 'completed' ? 'Chưa có nhiệm vụ hoàn thành' : filter === 'upcoming' ? 'Không có nhiệm vụ trong vòng 24h' : 'Chưa có nhiệm vụ'}</strong><span>${filter === 'all' ? 'Thêm nhiệm vụ đầu tiên của bạn.' : 'Không có nhiệm vụ phù hợp với bộ lọc này.'}</span></div>`}</div>` }; }

function openTaskModal() { const root = document.getElementById('appView'); if (!root || root.querySelector('.task-modal-backdrop')) return; const date = today(); root.insertAdjacentHTML('beforeend', `<div class="task-modal-backdrop" data-action="task-close-modal"><section class="task-modal" role="dialog" aria-modal="true" aria-labelledby="taskModalTitle"><form data-action="task-create" class="task-form"><header class="task-modal-header"><div><span class="eyebrow">NEW TASK</span><h2 id="taskModalTitle">Thêm nhiệm vụ</h2><p>Đặt khoảng thời gian, deadline và giờ hết hạn.</p></div><button type="button" class="icon-button" data-action="task-close-modal" aria-label="Đóng">×</button></header><div class="task-form-grid"><label class="task-field task-field-full"><span>Tên nhiệm vụ</span><input name="title" required maxlength="120" placeholder="Ví dụ: Hoàn thành bài tập Toán chương 3" autofocus></label><label class="task-field task-field-full"><span>Mô tả <small>(không bắt buộc)</small></span><textarea name="description" rows="3" maxlength="500" placeholder="Ghi chú ngắn về việc cần hoàn thành..."></textarea></label><label class="task-field"><span>Ngày bắt đầu</span><input type="date" name="startDate" value="${date}"></label><label class="task-field"><span>Deadline</span><input type="date" name="dueDate" value="${date}" required></label><label class="task-field"><span>Giờ deadline <small>(không bắt buộc)</small></span><input type="time" name="dueTime" value="23:59"><small class="field-hint">Trong 24 giờ cuối, ScholarOS sẽ hiển thị đếm ngược theo giờ/phút.</small></label><label class="task-field"><span>Độ ưu tiên</span><select name="priority"><option value="low">Thấp</option><option value="medium" selected>Trung bình</option><option value="high">Cao</option></select></label></div><div class="task-modal-footer"><button type="button" class="button" data-action="task-close-modal">Hủy</button><button type="submit" class="button primary">Thêm nhiệm vụ</button></div></form></section></div>`); root.querySelector('.task-modal input[name="title"]')?.focus(); }
function closeTaskModal() { document.querySelector('.task-modal-backdrop')?.remove(); }
export function handleTaskAction(action, id, event, target) { const state = getState(); if (action === 'task-add') { openTaskModal(); return; } if (action === 'task-close-modal') { const insideModal = event.target.closest('.task-modal'); if (!insideModal || event.target.closest('button[data-action="task-close-modal"]')) closeTaskModal(); return; } if (action === 'task-filter') { window.__scholarTaskFilter = target?.dataset.filter || 'all'; return 'refresh'; } if (action === 'task-create') { const form = target?.matches?.('form[data-action="task-create"]') ? target : event.target.closest('form[data-action="task-create"]'); if (!form) return; const data = new FormData(form), title = String(data.get('title') || '').trim(), startDate = String(data.get('startDate') || ''), dueDate = String(data.get('dueDate') || ''), dueTime = String(data.get('dueTime') || ''); if (!title) { form.querySelector('[name="title"]')?.focus(); return; } if (startDate && dueDate && dueDate < startDate) { alert('Deadline không thể sớm hơn ngày bắt đầu.'); return; } setState({ tasks: [...(state.tasks || []), { id: uid('task'), title, description: String(data.get('description') || '').trim(), priority: String(data.get('priority') || 'medium'), startDate: startDate || dueDate || today(), dueDate: dueDate || startDate || today(), dueTime, done: false }] }); closeTaskModal(); return 'refresh'; } if (action === 'task-toggle') { setState({ tasks: (state.tasks || []).map((task) => task.id === id ? { ...task, done: !task.done } : task) }); return 'refresh'; } if (action === 'task-delete') { setState({ tasks: (state.tasks || []).filter((task) => task.id !== id) }); return 'refresh'; } return null; }