import { bodyOf, cleanJson, generate, json, systemPrompt, aiErrorMessage } from './_lib/ai.js';
export default async function handler(req,res){ if(req.method!=='POST') return json(res,405,{error:'Method not allowed.'}); try { const {topic,subject,difficulty,count=5}=bodyOf(req); if(!String(topic||'').trim()) return json(res,400,{error:'Topic is required.'}); const n=Math.max(1,Math.min(10,Number(count)||5)); const result=await generate(`${systemPrompt()}\nCreate ${n} objectively gradable multiple-choice questions in Vietnamese about ${subject||'môn học'} / ${topic}. Difficulty: ${difficulty||'vừa'}. Return ONLY JSON: {title,questions:[{question,options:[A,B,C,D],answer:"A|B|C|D",explanation}]}. No markdown.`); return json(res,200,{ok:true,...cleanJson(result.text)}); } catch(err){ console.error('[AI quiz]',err); const e=aiErrorMessage(err); return json(res,e.status,{error:e.message}); } }
