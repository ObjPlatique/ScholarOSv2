import { bodyOf, cleanJson, generate, json, systemPrompt, aiErrorMessage } from './_lib/ai.js';
export default async function handler(req,res){ if(req.method!=='POST') return json(res,405,{error:'Method not allowed.'}); try { const context=JSON.stringify(bodyOf(req).context||{},null,2); const result=await generate(`${systemPrompt()}\nYou are a schedule optimizer. Keep fixed classes unchanged. Use 50/10 Pomodoro, meals, exercise and at least one long break. Return ONLY JSON: {summary:string,blocks:[{day,start,end,title,type,reason}],tasks:[{title,day,start,end}],warnings:[string]}. Suggest changes only; do not claim to modify data.\nCONTEXT:\n${context}`); return json(res,200,{ok:true,...cleanJson(result.text)}); } catch(err){ console.error('[AI schedule]',err); const e=aiErrorMessage(err); return json(res,e.status,{error:e.message}); } }
