import { bodyOf, cleanJson, generate, json, systemPrompt, aiErrorMessage } from './_lib/ai.js';
export default async function handler(req,res){ if(req.method!=='POST') return json(res,405,{error:'Method not allowed.'}); try { const {subject,question,studentAnswer,correctAnswer,work}=bodyOf(req); const result=await generate(`${systemPrompt()}\nAct as a careful teacher. Grade the student's work. Return ONLY JSON: {correct:boolean,score:number,feedback:string,errorLog:null|{subject,topic,question,cause,fix,review}}. Only create errorLog when there is a genuine mistake. Review should be one of "1 ngày","3 ngày","7 ngày","30 ngày".\nSUBJECT:${subject}\nQUESTION:${question}\nSTUDENT ANSWER:${studentAnswer}\nREFERENCE ANSWER:${correctAnswer||'not supplied'}\nWORK:${work||''}`); return json(res,200,{ok:true,...cleanJson(result.text)}); } catch(err){ console.error('[AI grade]',err); const e=aiErrorMessage(err); return json(res,e.status,{error:e.message}); } }
