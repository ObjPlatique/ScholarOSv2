import { bodyOf, cleanJson, generate, json, systemPrompt, aiErrorMessage } from './_lib/ai.js';
export default async function handler(req,res){ if(req.method!=='POST') return json(res,405,{error:'Method not allowed.'}); try { const context=JSON.stringify(bodyOf(req).context||{},null,2); const result=await generate(`${systemPrompt()}\nReturn ONLY JSON with key habits, an array of 3 objects {name,target,reason}. Suggest small sustainable habits based on this student context.\nCONTEXT:\n${context}`); return json(res,200,{ok:true,...cleanJson(result.text)}); } catch(err){ console.error('[AI habits]',err); const e=aiErrorMessage(err); return json(res,e.status,{error:e.message}); } }
